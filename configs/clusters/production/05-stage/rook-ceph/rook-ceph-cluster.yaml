---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rook-ceph-cluster
  namespace: rook-ceph
spec:
  interval: 15m
  install:
    timeout: 15m
  upgrade:
    timeout: 15m
  chart:
    spec:
      chart: rook-ceph-cluster
      sourceRef:
        kind: HelmRepository
        name: rook-ceph
  values:
    toolbox:
      enabled: true
    route:
      dashboard:
        host:
          name: rook-ceph.local.d20.fan
          pathType: PathPrefix
        parentRefs:
          - name: default
            namespace: nginx-gateway
            sectionName: https
    cephClusterSpec:
      mon:
        count: 1
      mgr:
        count: 1
      placement:
        osd:
          nodeAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
              nodeSelectorTerms:
              - matchExpressions:
                - key: node.kubernetes.io/instance-type
                  operator: In
                  values:
                  - cwwk-p5
                  - erying-14450hx
      resources:
        mgr:
          limits:
            memory: '4Gi'
        osd:
          requests:
            cpu: '500m'
            memory: '2Gi'
      dashboard:
        ssl: false
      security:
        cephx:
          daemon:
            keyRotationPolicy: KeyGeneration
            keyGeneration: 2
          # Rotate CSI keys to aes256k - requires kernel >= 7.0 on all nodes
          # (Talos currently ships 6.18.x, which cannot mount CephFS PVCs with
          # aes256k keys). Once rotated, set keepPriorKeyCountMax: 0 after all
          # existing volumes have remounted with the new key.
          #csi:
          #  keyRotationPolicy: KeyGeneration
          #  keyGeneration: 2
          #  keyType: aes256k
          #  keepPriorKeyCountMax: 1
      cephConfig:
        global:
          bdev_enable_discard: "true"
          bdev_async_discard_threads: "1"
          mon_data_avail_warn: "10"
    cephBlockPools:
    - name: builtin-mgr
      spec:
        name: .mgr
        failureDomain: osd
        replicated:
          size: 3
          requireSafeReplicaSize: true
        parameters:
          compression_mode: none
        mirroring:
          enabled: false
      storageClass:
        enabled: false
    cephFileSystems: []
    cephObjectStores: []
