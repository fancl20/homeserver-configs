{
   "apiVersion": "image.toolkit.fluxcd.io/v1beta2",
   "kind": "ImagePolicy",
   "metadata": {
      "name": "mongo",
      "namespace": "flux-system"
   },
   "spec": {
      "filterTags": {
         "pattern": ".*"
      },
      "imageRepositoryRef": {
         "name": "mongo"
      },
      "policy": {
         "semver": {
            "range": "8.*"
         }
      }
   }
}
