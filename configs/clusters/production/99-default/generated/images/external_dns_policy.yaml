{
   "apiVersion": "image.toolkit.fluxcd.io/v1beta2",
   "kind": "ImagePolicy",
   "metadata": {
      "name": "external_dns",
      "namespace": "flux-system"
   },
   "spec": {
      "imageRepositoryRef": {
         "name": "external_dns"
      },
      "policy": {
         "semver": {
            "range": "*"
         }
      }
   }
}
