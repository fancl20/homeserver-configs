{
   "apiVersion": "v1",
   "kind": "PersistentVolumeClaim",
   "metadata": {
      "name": "registry",
      "namespace": "default"
   },
   "spec": {
      "accessModes": [
         "ReadWriteMany"
      ],
      "resources": {
         "requests": {
            "storage": "10Gi"
         }
      },
      "volumeMode": "Filesystem"
   }
}
