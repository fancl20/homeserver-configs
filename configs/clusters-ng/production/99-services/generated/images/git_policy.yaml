{
   "apiVersion": "image.toolkit.fluxcd.io/v1beta2",
   "kind": "ImagePolicy",
   "metadata": {
      "name": "git",
      "namespace": "flux-system"
   },
   "spec": {
      "imageRepositoryRef": {
         "name": "git"
      },
      "policy": {
         "semver": {
            "range": "*"
         }
      }
   }
}
